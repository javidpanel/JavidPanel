# راهنمای نصب پنل جاوید

این راهنما برای کاربر عادی نوشته شده است و نصب را از ابتدا تا اولین اتصال توضیح می‌دهد.

## ۱. ساخت حساب کلادفلیر

اگر حساب کلادفلیر ندارید:

1. ربات رسمی پنل جاوید را باز کنید.
2. روی «ساخت حساب کلادفلیر» بزنید.
3. ایمیل خود را وارد کنید و یک رمز قوی برای حساب کلادفلیر بسازید.
4. حساب را ایجاد کنید.
5. ایمیل تأیید کلادفلیر را باز کنید و ایمیل خود را تأیید کنید.
6. دوباره وارد حساب کلادفلیر شوید.

برای نصب معمولی پنل جاوید، حساب رایگان کافی است و لازم نیست دامنه‌ای به حساب اضافه کنید.

[باز کردن ربات رسمی](https://t.me/javidpanelbot)

## ۲. ورکر چیست؟

پنل جاوید روی بخشی از کلادفلیر به نام «ورکر» نصب می‌شود. ورکر یک برنامه کوچک است که در شبکه کلادفلیر اجرا می‌شود. ربات نصب، این برنامه را برای شما می‌سازد و تنظیمات لازم را انجام می‌دهد.

برای نصب عادی لازم نیست سرور جداگانه بخرید یا تنظیمات لینوکس انجام دهید.

## ۳. ساخت توکن دسترسی

ربات برای ساخت پنل در حساب شما به یک توکن دسترسی محدود نیاز دارد.

1. در ربات روی «ساخت توکن دسترسی کلادفلیر» بزنید.
2. اگر لازم بود، وارد حساب کلادفلیر شوید.
3. دسترسی‌های موردنیاز در لینک آماده شده‌اند.
4. ساخت توکن را تأیید کنید.
5. کل رشته توکن را کپی کنید.
6. به ربات برگردید و «نصب پنل جدید» را بزنید.
7. توکن را در یک پیام برای ربات بفرستید.

توکن را برای فرد دیگری ارسال نکنید.

## ۴. نصب خودکار پنل

بعد از دریافت توکن، ربات آن را بررسی می‌کند. اگر چند حساب کلادفلیر داشته باشید، حساب موردنظر را انتخاب می‌کنید.

سپس ربات به‌صورت خودکار:

- فضای ذخیره‌سازی پنل را می‌سازد؛
- ورکر پنل را ایجاد می‌کند؛
- نشانی امن ورود را می‌سازد؛
- یک رمز مدیریت قوی ایجاد می‌کند؛
- نشانی ورود و رمز را برای شما می‌فرستد.

رمز مدیریت را همان لحظه در محل امن ذخیره کنید.

## ۵. ساخت اولین کاربر

1. نشانی پنل را باز کنید.
2. با نام کاربری و رمزی که ربات داده وارد شوید.
3. بخش «کاربران» را باز کنید.
4. روی «کاربر جدید» بزنید.
5. نام کاربر را وارد کنید.
6. مدت اعتبار را تعیین کنید.
7. در صورت نیاز محدودیت حجم مشخص کنید.
8. کاربر را ذخیره کنید.
9. صفحه همان کاربر را باز کنید.

صفحه کاربر شامل وضعیت، لینک اشتراک، کد تصویری و راهنمای اتصال است.

## ۶. اتصال روی گوشی با برنامه هپ

برای گوشی، برنامه هپ پیشنهاد اول است.

- [اندروید از گوگل‌پلی](https://play.google.com/store/apps/details?id=com.happproxy)
- [دریافت مستقیم نسخه اندروید](https://github.com/Happ-proxy/happ-android/releases/latest)
- [آیفون از اپ‌استور](https://apps.apple.com/us/app/happ-proxy-utility/id6504287215)

پس از نصب برنامه:

1. در صفحه کاربر روی «کپی لینک اصلی» بزنید.
2. برنامه هپ را باز کنید.
3. بخش اشتراک‌ها را باز کنید.
4. یک اشتراک جدید از نوع لینک ایجاد کنید.
5. لینک را وارد و ذخیره کنید.
6. اشتراک را بروزرسانی کنید.
7. یکی از اتصال‌ها را انتخاب و اتصال را فعال کنید.

## ۷. بروزرسانی پنل

برای بررسی بروزرسانی، ربات رسمی را باز کنید و «بررسی بروزرسانی پنل‌های من» را بزنید.

اگر نسخه جدیدی موجود باشد، دکمه بروزرسانی کنار پنل شما نمایش داده می‌شود. برای نصب نسخه جدید همان دکمه را بزنید.

[بررسی بروزرسانی در ربات رسمی](https://t.me/javidpanelbot?start=update)

## ۸. اگر پنل دچار مشکل شد

در «پنل‌های من» چند انتخاب دارید:

- «نصب دوباره» برای نصب مجدد روی همان پنل؛
- «نصب تمیز» برای ساخت پنل تازه با نشانی و رمز جدید؛
- «حذف پنل» برای حذف کامل نصب.

نصب تمیز اطلاعات پنل قبلی را منتقل نمی‌کند. قبل از استفاده از آن مطمئن شوید به اطلاعات قبلی نیاز ندارید.

## استفاده مجاز

پنل جاوید رایگان است و برای استفاده شخصی و خانوادگی ارائه می‌شود. فروش پنل، فروش دسترسی به آن یا ارائه نسخه پولی و تغییرنام‌داده‌شده مجاز نیست.
