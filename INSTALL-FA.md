# راهنمای نصب و استفاده پنل جاوید

پنل جاوید رایگان است و برای استفاده شخصی و خانوادگی ارائه می‌شود.

## ۱. ساخت حساب کلادفلیر

اگر حساب کلادفلیر ندارید، وارد صفحه رسمی ساخت حساب شوید:

https://dash.cloudflare.com/sign-up

ایمیل و یک رمز قوی وارد کنید، حساب را بسازید و سپس ایمیل تأیید کلادفلیر را باز کنید تا نشانی ایمیل شما تأیید شود.

## ۲. ورکر چیست؟

ورکر یک برنامه کوچک است که روی شبکه کلادفلیر اجرا می‌شود. پنل جاوید روی همین ورکر اجرا می‌شود، بنابراین برای نصب معمولی پنل نیازی به تهیه سرور جداگانه ندارید.

## ۳. نصب پنل

1. ربات رسمی [@javidpanelbot](https://t.me/javidpanelbot) را باز کنید.
2. «نصب پنل جدید» را بزنید.
3. از دکمه داخل ربات، توکن دسترسی کلادفلیر را بسازید.
4. توکن ساخته‌شده را کامل کپی کنید و برای ربات بفرستید.
5. اگر بیش از یک حساب کلادفلیر دارید، حساب موردنظر را انتخاب کنید.
6. ربات نصب را انجام می‌دهد و نشانی ورود به پنل و رمز مدیریت را نمایش می‌دهد.
7. رمز مدیریت را در جای امن نگه دارید.

## ۴. ساخت کاربر

بعد از ورود به پنل، بخش «کاربران» را باز کنید. برای هر عضو خانواده یا هر دستگاه می‌توانید یک کاربر جدا بسازید.

برای هر کاربر می‌توانید نام، مدت اعتبار و در صورت نیاز محدودیت حجم تعیین کنید. پس از ساخت، لینک اشتراک همان کاربر را کپی کنید و فقط برای همان شخص بفرستید.

## ۵. صفحه مشترک

هر کاربر یک صفحه مخصوص دارد که وضعیت اشتراک، تاریخ انقضا، حجم و لینک‌های قابل استفاده را نمایش می‌دهد. کاربر می‌تواند لینک مناسب برنامه خود را از همان صفحه کپی کند.

## ۶. نگهداری پنل

در ربات، بخش «پنل‌های من» را باز کنید:

- «بروزرسانی» نسخه عمومی جدید را روی همان پنل نصب می‌کند.
- «نصب دوباره» همان پنل را با همان نشانی و اطلاعات دوباره راه‌اندازی می‌کند.
- «نصب تمیز» یک پنل تازه با نشانی و رمز تازه می‌سازد و اطلاعات پنل قبلی را منتقل نمی‌کند.
- «حذف» پنل نصب‌شده و فضای ذخیره‌سازی مربوط به آن را حذف می‌کند.

## ۷. نکته مهم

پنل جاوید رایگان است و برای استفاده شخصی و خانوادگی طراحی شده است. فروش پنل، فروش دسترسی به آن یا ارائه نسخه تغییرنام‌داده‌شده به‌عنوان محصول پولی مجاز نیست.

کانال رسمی: [@javidpnl](https://t.me/javidpnl)
