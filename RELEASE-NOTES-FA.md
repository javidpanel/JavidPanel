# Release Notes — JavidPanel v0.2.1 Professional

## تغییرات v0.2.1

- فونت رابط مدیریت و پنل مشترک به Vazir تغییر یافت. فونت از CDN دریافت می‌شود و فایل فونت در بسته توزیع نشده است.
- Hero پنل مشترک بدون متن Overlay اضافه و با نسبت 16:9 نمایش داده می‌شود؛ نام و وضعیت مشترک به نوار مستقل زیر Hero منتقل شده است.
- نوار ابزار مدیریت کاربران برای دکمه «کاربر جدید»، جستجو و فیلتر در دسکتاپ و موبایل بازچینی شد.
- QR Codeهای پنل مشترک به 152px در دسکتاپ و تا 190px در موبایل افزایش یافتند.
- هسته ارتباطی، Router اصلی Core و مکانیزم تولید Subscription تغییر نکرده‌اند.


## انجام‌شده

- بازطراحی کامل Admin UI به رابط فشرده، ریسپانسیو و AJAX.
- داشبورد آماری و Workspace چندبخشی.
- مدیریت پیشرفته مشترکان و Modalهای عملیاتی.
- Date Picker شمسی داخلی و ایجاد کاربر با تعداد روز.
- تمدید روزانه از انتهای اعتبار، افزودن حجم، Reset Usage و Reset Token.
- تنظیمات دسته‌بندی‌شده Core 5.1.1 با راهنمای فارسی و Fallback خودکار برای کلیدهای آینده.
- مدیریت امنیت و Rate Limit در رابط گرافیکی.
- پنل مشترک جدید با Hero اختصاصی، QR، آمار حجم/زمان و راهنمای کلاینت‌ها.
- Hero به‌صورت Data URI داخل Worker Embed شد؛ فایل تصویر مجزا نیز برای آرشیو داخل بسته قرار دارد.
- راهنمای نصب مبتدی داخل پنل و فایل مستقل INSTALL-FA.md.

## اصل حفظ هسته

دو خط فیزیکی اول فایل مبنا که شامل SOURCE_CONTENT و کد Core/Bo هستند بدون تغییر بایت نگه داشته شده‌اند. لایه حرفه‌ای JavidPanel بعد از مرز هسته جایگزین لایه قبلی شده است.

## تست‌ها

- Syntax check کل Worker با Node در حالت ES Module: PASS.
- Syntax check JavaScript تولیدشده داخل Admin HTML: PASS.
- Syntax check JavaScript تولیدشده داخل Subscriber HTML: PASS.
- تست تبدیل Gregorian ↔ Jalali برای 2026-03-21 ↔ 1405/01/01: PASS.
- تست رد تاریخ نامعتبر 1405/13/01: PASS.
- تست محاسبه ۳۰ روز از 1405/01/01 → 1405/01/31: PASS.
- بررسی Metadata تنظیمات: 84/84 کلید شناخته‌شده پوشش داده شده است: PASS.
- مقایسه بایت‌به‌بایت خطوط هسته با فایل مبنا: PASS (جزئیات در INTEGRITY.txt).

- گزارش تست جزئیات این نسخه در `TEST-REPORT.txt` قرار دارد.
