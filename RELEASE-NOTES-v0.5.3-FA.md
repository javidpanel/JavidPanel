# انتشار 0.5.3

این نسخه یک اصلاح نگهداری برای Installer است. خطای JavaScript صفحه `/admin` که باعث غیرفعال شدن فرم ورود می‌شد برطرف شده، favicon داخلی اضافه شده و نام محصول در پیام‌های کاربری ربات به «پنل جاوید» یکدست شده است.

برای ارتقا، `BOT_KV` و `MASTER_KEY` فعلی را حفظ کنید و Worker Installer را جایگزین کنید.
