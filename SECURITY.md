# امنیت جاویدپنل

امنیت حساب Cloudflare، اطلاعات ورود پنل و لینک‌های اشتراک مستقیماً به نحوه نگهداری آن‌ها وابسته است. این نکات را جدی بگیرید.

## فقط منابع رسمی

- 🤖 ربات: `@javidpanelbot`
- 📢 کانال: `@javidpnl`
- 💻 GitHub: `github.com/JavidPanel/JavidPanel`

برای نصب و بروزرسانی از لینک یا فایل ناشناس استفاده نکنید.

## Cloudflare API Token

- برای نصب از **Global API Key** استفاده نکنید.
- Token را از لینک آماده‌شده داخل ربات رسمی بسازید.
- Token را برای افراد، گروه‌ها یا ربات‌های دیگر ارسال نکنید.
- اگر به Token مشکوک شدید، آن را از Cloudflare Dashboard لغو و Token جدید ایجاد کنید.

## رمز پنل

- از رمز قوی و منحصربه‌فرد استفاده کنید.
- رمز را داخل پیام عمومی، Issue یا تصویر بدون پوشش منتشر نکنید.
- نگهداری رمز در Password Manager توصیه می‌شود.

## لینک اشتراک و QR

لینک اشتراک مانند یک Credential است. هر شخص فقط باید لینک مخصوص خود را دریافت کند. لینک کامل یا QR را در کانال عمومی، اسکرین‌شات یا Issue GitHub منتشر نکنید.

## مدیریت کاربران از ربات

قبل از انجام عملیات، پنل صحیح و نام کاربر را بررسی کنید. عملیات حذف را فقط بعد از کنترل مشخصات کاربر تأیید کنید.

## گزارش مشکل

اگر مشکلی شامل Token، رمز، لینک اشتراک یا داده شخصی است، آن را در Issue عمومی GitHub قرار ندهید و از پشتیبانی رسمی پروژه استفاده کنید.

[English security guide](SECURITY-EN.md)
