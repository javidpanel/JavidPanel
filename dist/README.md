# Production distribution

`JavidPanel.worker.js` and `manifest.json` are created only after the release Wrapper passes the approved Obfuscator.io pipeline and smoke tests. Do not publish the readable owner build in this directory.
