# Production distribution

`JavidPanel.worker.js` appears here only after the private wrapper has been manually obfuscated, uploaded to Installer Admin, assembled with the preserved Core, validated as Beta, and explicitly promoted to Public.

Until that point `manifest.json` remains in `awaiting-manual-obfuscation` state and no production Worker is published.
