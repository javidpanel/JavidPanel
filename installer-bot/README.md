# نصاب پنل جاوید — نسخه 0.5.4

این بسته برای نصب ربات رسمی پنل جاوید روی کلادفلیر آماده شده است.

برای نصب تازه، فایل `INSTALL-FA.md` را بخوانید. اگر نسخه قبلی نصب است، فضای ذخیره‌سازی و کلید اصلی فعلی را حفظ کنید و فقط کد ورکر نصاب را بروزرسانی کنید.
