# روش انتشار نسخه پنل

برای هر نسخه، یک فایل کامل `worker.js` از بسته مالک پروژه گرفته می‌شود. این فایل به‌صورت دستی مبهم می‌شود و خروجی مبهم‌شده به‌عنوان نسخه آزمایشی در پنل نصاب بارگذاری می‌شود. پس از آزمایش و تأیید، نسخه برای نصب عمومی فعال می‌شود.
